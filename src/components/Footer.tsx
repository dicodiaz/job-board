import { Container } from 'react-bootstrap';

const Footer = () => {
  return (
    <Container fluid as="footer" className="fixed-bottom py-2 text-center">
      <small>Footer Placeholder</small>
    </Container>
  );
};

export default Footer;
